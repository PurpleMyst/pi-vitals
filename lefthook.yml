pre-commit:
  parallel: false
  commands:
    typecheck:
      run: pnpm typecheck
      glob:
        - "src/**/*.ts"
        - "tsconfig.json"

    build_dist:
      run: pnpm build && git add dist/extension.js
      glob:
        - "src/**/*.ts"
        - "package.json"
        - "pnpm-lock.yaml"
        - "tsconfig.json"
